<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.PortFolioMapper">

    <!-- 全件検索 -->
    <select id="findAll" resultType="com.example.demo.entity.PortFolio">
        SELECT * FROM users
    </select>

    <!-- 主キー検索 -->
    <select id="findById" resultType="com.example.demo.entity.PortFolio">
        SELECT * FROM users WHERE id=#{id}
    </select>

    <!-- 条件指定検索 -->
    <select id="search" resultType="com.example.demo.entity.PortFolio">
        SELECT
            *
        FROM
            users
            <if test="id != null and id != ''">
                AND id = #{id}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
    </select>

    <!-- 新規登録 -->
    <insert id="save">
        INSERT INTO users
            (email, name, self_introduction, password, created_at, updated_at)
        VALUES
            (#[email], #{name}, #{self_introduction},#[password], CURRENT_TIMESTAMP,  CURRENT_TIMESTAMP)
    </insert>

    <!-- 更新 -->
    <update id="update">
        UPDATE users SET email = #[email], name = #{name}, self_introduction = #{self_introduction}, 
        password = #{password}, updated_at = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

</mapper>